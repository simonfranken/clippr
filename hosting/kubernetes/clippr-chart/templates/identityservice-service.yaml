apiVersion: v1
kind: Service
metadata:
  name: clippr-identityservice
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.identityServicePort }}
      targetPort: 8080
      name: http
  selector:
    app: clippr-identityservice
